import React from "react";
import { TransactionData } from "../types";

type Props = {
  transactionData: TransactionData[];
};

function ResultTab({ transactionData }: Props) {
  return <div>ResultTab</div>;
}

export default ResultTab;
