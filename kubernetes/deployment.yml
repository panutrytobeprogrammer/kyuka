apiVersion: apps/v1
kind: Deployment
metadata:
  namespace: witeseb
  name: kyuka-web-dev
spec:
  replicas: 1
  selector:
    matchLabels:
      app: kyuka-web-dev
  template:
    metadata:
      labels:
        app: kyuka-web-dev
    spec:
      imagePullSecrets:
        - name: regcred
      containers:
        - name: kyuka-web-dev
          image: registry.witeseb.website/kyuka:dev
          imagePullPolicy: Always
          ports:
            - containerPort: 3000
          resources:
            limits:
              cpu: 200m
              memory: 256Mi
            requests:
              cpu: 100m
              memory: 128Mi
          envFrom:
            - secretRef:
                name: kyuka-web-secret
