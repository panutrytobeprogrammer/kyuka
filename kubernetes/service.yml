apiVersion: v1
kind: Service
metadata:
  name: kyuka-web-svc
  namespace: witeseb
spec:
  ports:
    - protocol: TCP
      port: 3000
      targetPort: 3000
  selector:
    app: kyuka-web
  type: ClusterIP
